// Coloque aqui os @ (sem o @) das contas do X que o agente deve monitorar
export const ACCOUNTS_TO_MONITOR = [
  'g1',
  'CNNBrasil',
];

// Quantos posts recentes olhar por conta a cada verificação
export const MAX_TWEETS_PER_ACCOUNT = 5;
